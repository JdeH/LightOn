all_names = ['Mick', 'Bonny', 'Herbie', 'Bono', 'Ella', 'Ray', 'Barbara']   # Create name list
print ('All names:', all_names)

filtered_names = [name for name in all_names if name [0] == 'B']            # Select names starting with B
print ('Filtered names:', filtered_names)